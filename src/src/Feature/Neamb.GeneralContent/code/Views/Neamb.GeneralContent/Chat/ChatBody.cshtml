@using Neambc.Neamb.Feature.GeneralContent
@using Neambc.Neamb.Foundation.Config.Models

@model Neambc.Neamb.Feature.GeneralContent.Models.ChatDTO
@{
	bool isExperienceEditorPreview = Sitecore.Context.PageMode.IsExperienceEditor || Sitecore.Context.PageMode.IsPreview;
}
@if (!isExperienceEditorPreview)
{
	@Model.DefaultChat
}